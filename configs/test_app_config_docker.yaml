flask:
  host: "localhost"
  port: 5001

directories:
  source_directory: "/app/tests/fixtures/images"
  target_directory: "/app/tests/fixtures/incoming_images"
  processed_directory: "/app/tests/fixtures/incoming_images/processed"

db_path: "/app/tests/fixtures/test_image_classifications.db"

model_path: "/app/model/trained_enet_0.onnx"

batch:
  min_images_per_run: 1
  max_images_per_run: 10

train_data:
  path: "/app/src/model/model_training/data"
